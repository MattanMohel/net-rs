pub mod mat;
pub mod num;
// pub mod net;
// pub mod act;
// pub mod cost;
// pub mod init;

use mat::Mat;
use mat::MatErr;
use num::Num;

use rand::prelude::*;

use std::ops::Mul;

fn main() -> Result<(), MatErr> {
    Ok(())
}